# Power Automate 流程設定指南

本文件說明如何建立 Power Automate 流程來自動化專案管理工作流程。

---

## 流程概覽

我們將建立以下 4 個自動化流程：

1. **新任務通知流程** - 當 Planner 新增任務時自動通知
2. **任務到期提醒流程** - 每日檢查並提醒即將到期的任務
3. **週報自動生成流程** - 每週五自動生成並發送週報
4. **任務完成同步流程** - 任務完成時同步更新 Excel

---

## 流程 1: 新任務通知

### 目的
當 Planner 中新增任務時，自動發送通知給負責人和團隊。

### 觸發條件
- **觸發器**: 當任務建立時 (Planner)
- **頻率**: 即時

### 流程步驟

#### 步驟 1: 觸發器設定
```
觸發器: When a task is created (Planner)
- Group Id: [選擇您的 Teams 團隊]
- Plan Id: [選擇您的 Planner 計畫]
```

#### 步驟 2: 取得任務詳細資訊
```
動作: Get task details (Planner)
- Task Id: [從觸發器取得]
```

#### 步驟 3: 取得負責人資訊
```
動作: Get user profile (V2) (Office 365 Users)
- User (UPN): [從任務詳細資訊取得]
```

#### 步驟 4: 發送 Teams 訊息
```
動作: Post message in a chat or channel (Teams)
- Post as: Flow bot
- Post in: Channel
- Team: [您的團隊]
- Channel: 任務追蹤
- Message:
  📋 新任務通知
  
  任務名稱: [任務標題]
  負責人: @[負責人姓名]
  截止日期: [到期日期]
  優先級: [標籤]
  
  請前往 Planner 查看詳細資訊。
```

#### 步驟 5: 發送 Email 給負責人
```
動作: Send an email (V2) (Office 365 Outlook)
- To: [負責人 Email]
- Subject: 新任務指派：[任務標題]
- Body:
  您好，
  
  您已被指派一個新任務：
  
  任務名稱: [任務標題]
  截止日期: [到期日期]
  優先級: [標籤]
  
  請登入 Microsoft Planner 查看詳細資訊並開始執行。
  
  專案管理系統自動通知
```

#### 步驟 6: 新增至 To Do
```
動作: Create a task (To Do)
- List Id: 我的任務
- Title: [任務標題]
- Due Date: [到期日期]
- Reminder Date Time: [到期日期 - 1 天]
```

### 測試方式
1. 在 Planner 中新增一個測試任務
2. 檢查是否收到 Teams 訊息
3. 檢查是否收到 Email
4. 檢查 To Do 是否新增任務

---

## 流程 2: 任務到期提醒

### 目的
每日早上 9:00 檢查即將到期的任務，並發送提醒。

### 觸發條件
- **觸發器**: 排程 (Recurrence)
- **頻率**: 每日早上 9:00

### 流程步驟

#### 步驟 1: 觸發器設定
```
觸發器: Recurrence
- Interval: 1
- Frequency: Day
- Time zone: (UTC+08:00) Taipei
- At these hours: 9
- At these minutes: 0
```

#### 步驟 2: 取得所有任務
```
動作: List tasks (Planner)
- Group Id: [您的團隊]
- Plan Id: [您的計畫]
```

#### 步驟 3: 篩選即將到期任務
```
動作: Filter array
- From: [任務清單]
- Condition:
  - Due Date is less than or equal to addDays(utcNow(), 3)
  - AND Percent Complete is not equal to 100
```

#### 步驟 4: 對每個任務執行
```
動作: Apply to each
- Select an output from previous steps: [篩選後的任務]
```

#### 步驟 5: 取得負責人資訊
```
動作: Get user profile (V2)
- User (UPN): [任務負責人]
```

#### 步驟 6: 計算剩餘天數
```
動作: Compose
- Inputs: div(sub(ticks(item()?['dueDateTime']), ticks(utcNow())), 864000000000)
```

#### 步驟 7: 發送提醒 Email
```
動作: Send an email (V2)
- To: [負責人 Email]
- Subject: ⚠️ 任務即將到期提醒：[任務標題]
- Body:
  您好，
  
  以下任務即將到期，請盡快處理：
  
  任務名稱: [任務標題]
  截止日期: [到期日期]
  剩餘天數: [剩餘天數] 天
  目前進度: [完成百分比]%
  
  請登入 Planner 更新任務進度。
  
  專案管理系統自動提醒
- Importance: High
```

#### 步驟 8: 發送 Teams 訊息
```
動作: Post message in a chat or channel
- Team: [您的團隊]
- Channel: 任務追蹤
- Message:
  ⚠️ 任務即將到期
  
  任務: [任務標題]
  負責人: @[負責人]
  截止日期: [到期日期]
  剩餘: [剩餘天數] 天
```

---

## 流程 3: 週報自動生成

### 目的
每週五下午 5:00 自動生成週報並發送給團隊。

### 觸發條件
- **觸發器**: 排程 (Recurrence)
- **頻率**: 每週五下午 5:00

### 流程步驟

#### 步驟 1: 觸發器設定
```
觸發器: Recurrence
- Interval: 1
- Frequency: Week
- On these days: Friday
- At these hours: 17
- At these minutes: 0
```

#### 步驟 2: 初始化變數
```
動作: Initialize variable
- Name: weekStart
- Type: String
- Value: formatDateTime(addDays(utcNow(), -7), 'yyyy-MM-dd')

動作: Initialize variable
- Name: weekEnd
- Type: String
- Value: formatDateTime(utcNow(), 'yyyy-MM-dd')
```

#### 步驟 3: 取得所有任務
```
動作: List tasks (Planner)
- Group Id: [您的團隊]
- Plan Id: [您的計畫]
```

#### 步驟 4: 篩選本週完成任務
```
動作: Filter array
- From: [任務清單]
- Condition:
  - Percent Complete equals 100
  - AND Completed Date Time is greater than or equal to [weekStart]
```

#### 步驟 5: 計算統計數據
```
動作: Compose - 本週完成數
- Inputs: length(body('篩選本週完成任務'))

動作: Compose - 總任務數
- Inputs: length(body('取得所有任務'))

動作: Compose - 完成率
- Inputs: div(mul(length(body('篩選本週完成任務')), 100), length(body('取得所有任務')))
```

#### 步驟 6: 篩選逾期任務
```
動作: Filter array
- From: [任務清單]
- Condition:
  - Due Date is less than utcNow()
  - AND Percent Complete is not equal to 100
```

#### 步驟 7: 建立 HTML 表格 - 逾期任務
```
動作: Create HTML table
- From: [逾期任務]
- Columns: Automatic
- Include headers: Yes
```

#### 步驟 8: 發送週報 Email
```
動作: Send an email (V2)
- To: [團隊成員 Email 清單]
- Subject: 📊 週報 - [weekStart] 至 [weekEnd]
- Body:
  <h2>I&C Project Management 週報</h2>
  
  <h3>📊 本週關鍵指標</h3>
  <ul>
    <li>本週完成任務數: [本週完成數]</li>
    <li>總任務數: [總任務數]</li>
    <li>整體完成率: [完成率]%</li>
    <li>逾期任務數: [逾期任務數]</li>
  </ul>
  
  <h3>🔴 逾期任務清單</h3>
  [逾期任務 HTML 表格]
  
  <p>詳細資訊請查看 Dashboard.xlsx</p>
  
  <p>此郵件由系統自動發送</p>
```

#### 步驟 9: 發送 Teams 訊息
```
動作: Post message in a chat or channel
- Team: [您的團隊]
- Channel: 週報與檢討
- Message:
  📊 本週週報已發送
  
  本週完成: [本週完成數] 個任務
  整體進度: [完成率]%
  逾期任務: [逾期任務數] 個
  
  請查看 Email 了解詳細資訊。
```

---

## 流程 4: 任務完成同步

### 目的
當 Planner 任務標記為完成時，自動更新 Excel 並發送通知。

### 觸發條件
- **觸發器**: 當任務更新時 (Planner)
- **頻率**: 即時

### 流程步驟

#### 步驟 1: 觸發器設定
```
觸發器: When a task is completed (Planner)
- Group Id: [您的團隊]
- Plan Id: [您的計畫]
```

#### 步驟 2: 條件判斷
```
動作: Condition
- If: Percent Complete equals 100
```

#### 步驟 3: 取得任務詳細資訊
```
動作: Get task details (Planner)
- Task Id: [從觸發器取得]
```

#### 步驟 4: 更新 Excel 行
```
動作: Update a row (Excel Online)
- Location: OneDrive for Business
- Document Library: OneDrive
- File: Dashboard.xlsx
- Table: 任務清單
- Key Column: 任務ID
- Key Value: [任務 ID]
- 狀態: 已完成
- 完成日期: utcNow()
- 進度%: 100
```

#### 步驟 5: 從 To Do 移除
```
動作: Delete a task (To Do)
- List Id: 我的任務
- Task Id: [對應的 To Do 任務 ID]
```

#### 步驟 6: 發送完成通知
```
動作: Post message in a chat or channel
- Team: [您的團隊]
- Channel: 任務追蹤
- Message:
  ✅ 任務已完成
  
  任務名稱: [任務標題]
  負責人: [負責人]
  完成日期: [今日日期]
  
  恭喜完成任務！
```

#### 步驟 7: 發送 Email 給專案經理
```
動作: Send an email (V2)
- To: [專案經理 Email]
- Subject: ✅ 任務完成通知：[任務標題]
- Body:
  任務已完成：
  
  任務名稱: [任務標題]
  負責人: [負責人]
  原定截止日: [到期日期]
  實際完成日: [今日日期]
  
  Excel Dashboard 已自動更新。
```

---

## 建立流程步驟

### 1. 登入 Power Automate

1. 前往 https://make.powerautomate.com
2. 使用您的 Microsoft 365 帳號登入

### 2. 建立新流程

1. 點擊「建立」
2. 選擇「自動化雲端流程」或「排程的雲端流程」
3. 輸入流程名稱
4. 選擇觸發器

### 3. 新增動作

1. 點擊「+ 新步驟」
2. 搜尋並選擇動作
3. 設定動作參數
4. 使用動態內容連接資料

### 4. 測試流程

1. 點擊「測試」
2. 選擇「手動」或「自動」
3. 執行測試
4. 檢查執行記錄

### 5. 啟用流程

1. 確認測試成功
2. 點擊「開啟」啟用流程
3. 監控流程執行狀態

---

## 進階設定

### 錯誤處理

在每個重要動作後新增：

```
動作: Configure run after
- 設定: is successful, has failed, is skipped, has timed out
```

新增錯誤通知：

```
動作: Send an email (V2)
- To: [管理員 Email]
- Subject: ⚠️ Power Automate 流程錯誤
- Body: 流程 [流程名稱] 執行失敗，請檢查。
```

### 條件分支

使用條件動作來處理不同情境：

```
動作: Condition
- If: [條件]
- Yes: [執行動作 A]
- No: [執行動作 B]
```

### 迴圈處理

使用 Apply to each 處理多個項目：

```
動作: Apply to each
- Select an output: [陣列]
- Actions: [對每個項目執行的動作]
```

---

## 疑難排解

### 問題 1: 流程未觸發

**可能原因**:
- 觸發器設定錯誤
- 權限不足
- 流程未啟用

**解決方式**:
1. 檢查觸發器設定
2. 確認權限
3. 確認流程已開啟

### 問題 2: 動作執行失敗

**可能原因**:
- 參數設定錯誤
- 連接器權限不足
- 資料格式不符

**解決方式**:
1. 檢查執行記錄
2. 查看錯誤訊息
3. 調整參數設定

### 問題 3: Email 未收到

**可能原因**:
- Email 地址錯誤
- 被垃圾郵件過濾
- 流程執行失敗

**解決方式**:
1. 確認 Email 地址
2. 檢查垃圾郵件資料夾
3. 查看流程執行記錄

---

## 最佳實踐

### 1. 命名規範

- 流程名稱: `[專案名稱] - [功能描述]`
- 動作名稱: 使用描述性名稱
- 變數名稱: 使用 camelCase

### 2. 註解說明

在複雜的動作前新增註解：

```
動作: Compose
- Inputs: [註解說明此動作的目的]
```

### 3. 測試驗證

- 建立測試環境
- 使用測試資料
- 記錄測試結果

### 4. 監控維護

- 定期檢查執行記錄
- 監控失敗率
- 及時修正錯誤

### 5. 文件記錄

- 記錄流程設計
- 記錄變更歷史
- 記錄已知問題

---

## 權限設定

### 必要權限

1. **Planner**: 讀取和寫入
2. **Teams**: 發送訊息
3. **Outlook**: 發送郵件
4. **Excel Online**: 讀取和寫入
5. **To Do**: 建立和刪除任務

### 設定方式

1. 前往 Power Automate
2. 點擊「資料」> 「連線」
3. 新增或編輯連線
4. 授予必要權限

---

## 總結

透過這 4 個 Power Automate 流程，您可以：

1. ✅ 自動通知新任務
2. ⏰ 自動提醒到期任務
3. 📊 自動生成週報
4. 🔄 自動同步任務狀態

這將大幅提升專案管理效率，減少手動作業時間。

---

**文件版本**: 1.0  
**建立日期**: 2025-12-30  
**適用範圍**: Microsoft 365 環境  
**前置需求**: Power Automate 授權
